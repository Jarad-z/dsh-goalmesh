/** Inert Node entry for the installable GoalMesh composition bundle. */
export const name = 'goalmesh-bundle'

/** Bundle packages contribute configuration; the runtime entries live in sibling packages. */
export function apply() {}
