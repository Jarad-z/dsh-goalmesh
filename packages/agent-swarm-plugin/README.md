# DSH Agent Swarm Plugin

Installable DeepSeek Harness composition bundle. It inserts the Agent Swarm Host tool,
its invariant companion, and the Web trajectory companion as one configuration layer.

The bundle implements the v0.1 fixed-batch foreground scheduler, durable trajectory,
trajectory invariant, and Web Agent tree as one installable composition.
