/** Cordis services required by the Agent Swarm browser companion. */
export const inject = ['conversationEvents', 'slots', 'sessions', 'locale']

/** Wiring-only browser body; trajectory projection and rendering land with v0.1. */
export function apply(): void {}
