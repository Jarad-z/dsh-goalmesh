/** Agent Swarm invariant companion scaffold. */
export const name = 'agent-swarm-invariant'

/** Wiring-only lifecycle body; durable trajectory checks land with the recorder. */
export function apply(): void {}
